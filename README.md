# 图片分类——手写数字识别（监督学习，改进版）

对应浙江信息技术高考考点：**监督学习、训练集/测试集划分、计算机视觉、模型评估**。

## 做了什么
使用 sklearn 自带的手写数字数据集（1797张 8×8 手写数字图片，标签0~9），
用 **SVM支持向量机** 分类器训练模型。训练完成后，用户可以通过输入图片文件路径，
让程序识别图片中的手写数字并输出结果。

## 核心流程（与高考考点对应）
1. 加载带标签的数据集 → **监督学习**（数据有标签）
2. 划分训练集(70%)和测试集(30%) → **数据集划分**（高考高频考点）
3. 用训练集训练SVM模型 → **模型训练**
4. 用测试集预测，计算准确率 → **模型评估**
5. 混淆矩阵查看哪些数字容易认错 → **错误分析**
6. 用户输入图片路径 → 图片预处理 → 模型识别 → 输出结果

## 改进内容（提升识别成功率）
| 改进项 | 之前 | 现在 | 效果 |
| --- | --- | --- | --- |
| 模型 | KNN近邻 | SVM支持向量机 | 测试集准确率 98.89% → 99.07% |
| 图片缩放 | 直接整张图缩放到8×8 | 先找数字边界框裁剪，再缩放 | 数字偏移/大小不一时也能正确识别 |
| 缩放尺寸 | 最大边6~7像素 | 高度占满8像素，宽度按比例 | 和digits数据集分布一致 |
| 插值方式 | NEAREST（纯黑白锯齿） | BILINEAR（保留灰度渐变） | 和digits数据集灰度特征匹配 |
| 二值化 | 有（丢失灰度信息） | 无（保留灰度渐变） | 预处理后特征和训练数据一致 |

## 识别率测试结果
- **10张示例图片**：10/10 正确（100%）
- **21张用户风格图片**（偏移、大小不一、白底黑字）：20/21 正确（95.2%）
- **digits测试集**：540张，准确率 99.07%

## 新增功能：用户输入图片识别
- 程序运行后，提示用户输入图片文件路径
- 支持 PNG、JPG 等常见图片格式
- 自动判断白底黑字/黑底白字，自动反色处理
- 自动裁剪空白、缩放、居中，和训练数据格式一致
- 输入 `quit` 退出程序

## 文件结构
```
A_image_classification/
├── classify.py                 # 主程序：SVM训练 + 用户输入图片识别
├── generate_sample_images.py   # 生成示例数字图片（0~9各一张）
├── requirements.txt
├── README.md
├── sample_images/              # 示例数字图片（运行generate脚本后生成）
│   ├── digit_0.png ~ digit_9.png
└── output/
    └── digits_result.png       # 混淆矩阵图
```

## 运行步骤
```
# 1. 安装依赖
pip install -r requirements.txt

# 2. 生成示例数字图片（只需运行一次）
python generate_sample_images.py

# 3. 运行主程序
python classify.py

# 4. 程序提示输入图片路径时，输入示例图片路径，例如：
sample_images/digit_5.png

# 5. 输入 quit 退出
```

## 自己画图的注意事项（提高识别率）
1. **画布**：正方形（如300×300像素），数字尽量居中
2. **颜色**：推荐黑底白字（和训练数据一致），白底黑字也支持（自动反色）
3. **画笔**：粗画笔，笔画连续，不要太细
4. **大小**：数字占画布60%~80%，不要太小（压缩后丢失特征），不要顶到边缘
5. **单数字**：一张图片只能写一个数字
6. **保存**：PNG格式优先
7. **摆正**：不要倾斜过大

## 输出
- 控制台：数据集大小、训练/测试集数量、准确率、混淆矩阵
- 交互提示：输入图片路径后，输出识别结果（如"✅ 识别结果：数字 5"）
- `output/digits_result.png`：混淆矩阵图

## 高考生学习要点
- 只用了 sklearn 的：`load_digits`、`train_test_split`、`SVC`、`accuracy_score`、`confusion_matrix`
- 图片预处理用了 PIL（`Image.open`、`convert`、`resize`）和 numpy
- 没有神经网络、没有深度学习，全部在考纲范围内
- 关键概念：训练集用来"教"，测试集用来"考"，两者不能混用
- 图片预处理是计算机视觉的重要步骤：用户图片必须转成和训练数据一致的格式，模型才能识别
- SVM（支持向量机）是监督学习分类器的一种，在高维数据上效果好
